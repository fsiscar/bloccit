New post created: "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

by: <%= @post.user.name %>

<%= @post.body %>

Visit online at: <%=topic_post_url(@post.topic, anchor: "comment-#{@post.id}") %>
